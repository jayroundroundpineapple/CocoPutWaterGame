# 拼图游戏关卡配置说明

## 概述

拼图游戏现在支持动态关卡配置，每关可以设置不同的网格大小（如 2x2、3x3、4x4、5x5 等）。

## 配置文件

配置文件位于：`assets/resources/config/puzzle-levels.json`

### JSON 格式

```json
{
    "levels": [
        {
            "level": 1,
            "rows": 2,
            "cols": 2,
            "imagePath": "images/puzzle/puzzle1/pic/spriteFrame"
        },
        {
            "level": 2,
            "rows": 3,
            "cols": 3,
            "imagePath": "images/puzzle/puzzle2/pic/spriteFrame"
        },
        {
            "level": 3,
            "rows": 4,
            "cols": 4,
            "imagePath": "images/puzzle/puzzle3/pic/spriteFrame"
        }
    ]
}
```

### 字段说明

- **level**: 关卡编号（从 1 开始）
- **rows**: 行数（拼图块的行数）
- **cols**: 列数（拼图块的列数）
- **imagePath**: 图片资源路径（相对于 `resources` 目录）

### 示例配置

#### 第一关：2x2（4 块）
```json
{
    "level": 1,
    "rows": 2,
    "cols": 2,
    "imagePath": "images/puzzle/puzzle1/pic/spriteFrame"
}
```

#### 第二关：3x3（9 块）
```json
{
    "level": 2,
    "rows": 3,
    "cols": 3,
    "imagePath": "images/puzzle/puzzle2/pic/spriteFrame"
}
```

#### 第三关：4x4（16 块）
```json
{
    "level": 3,
    "rows": 4,
    "cols": 4,
    "imagePath": "images/puzzle/puzzle3/pic/spriteFrame"
}
```

#### 第四关：5x5（25 块）
```json
{
    "level": 4,
    "rows": 5,
    "cols": 5,
    "imagePath": "images/puzzle/puzzle4/pic/spriteFrame"
}
```

## 使用方法

### 1. 创建配置文件

在 Cocos Creator 编辑器中：
1. 在 `assets/resources/config/` 目录下创建 `puzzle-levels.json` 文件
2. 按照上述格式填写关卡配置

### 2. 确保资源路径正确

- 图片资源必须放在 `assets/resources/` 目录下
- `imagePath` 应该是相对于 `resources` 目录的路径
- 例如：如果图片在 `assets/resources/images/puzzle/puzzle1/pic/spriteFrame`，则 `imagePath` 为 `"images/puzzle/puzzle1/pic/spriteFrame"`

### 3. 运行游戏

游戏会自动从 JSON 文件加载关卡配置，无需修改代码。

## 代码改动说明

### PuzzleManager.ts

- 添加了 `LevelConfig` 接口定义
- 添加了 `loadLevelConfigs()` 方法从 JSON 加载配置
- 修改了 `initPositions()` 方法，支持动态 `rows` 和 `cols`
- 修改了 `createPieces()` 方法，根据 `rows * cols` 创建拼图块
- 修改了 `nextLevel()` 方法，从配置中读取下一关信息

### PuzzlePiece.ts

- 修改了 `init()` 方法，添加 `rows` 和 `cols` 参数
- 修改了 `createCroppedSpriteFrame()` 方法，支持动态网格大小

## 注意事项

1. **JSON 文件位置**：必须放在 `assets/resources/config/` 目录下
2. **资源路径**：`imagePath` 必须是相对于 `resources` 目录的路径
3. **关卡编号**：`level` 必须从 1 开始，且连续递增
4. **网格大小**：`rows` 和 `cols` 建议不要太大（如超过 10x10），否则拼图块会很小，难以操作
5. **默认配置**：如果 JSON 加载失败，系统会使用默认配置（2x2, 3x3, 4x4）

## 故障排除

### 问题：关卡配置未加载

**解决方案**：
1. 检查 JSON 文件是否在 `assets/resources/config/` 目录下
2. 检查 JSON 格式是否正确（可以使用在线 JSON 验证工具）
3. 查看控制台日志，确认是否有加载错误

### 问题：图片加载失败

**解决方案**：
1. 检查 `imagePath` 是否正确
2. 确认图片资源已正确导入到 `resources` 目录
3. 检查资源路径中是否包含文件扩展名（通常不需要）

### 问题：拼图块显示不正确

**解决方案**：
1. 确认 `rows` 和 `cols` 配置正确
2. 检查图片资源是否正确
3. 查看控制台日志，确认拼图块创建数量是否正确

## 扩展功能

### 添加更多关卡

只需在 JSON 文件的 `levels` 数组中添加新的关卡配置即可：

```json
{
    "levels": [
        // ... 现有关卡 ...
        {
            "level": 4,
            "rows": 5,
            "cols": 5,
            "imagePath": "images/puzzle/puzzle4/pic/spriteFrame"
        }
    ]
}
```

### 自定义网格大小

可以设置任意 `rows` 和 `cols` 值，例如：
- 2x3（6 块）
- 3x4（12 块）
- 4x5（20 块）

注意：拼图块数量 = `rows * cols`

