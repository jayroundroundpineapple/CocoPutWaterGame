# 拼图游戏使用说明

## 功能概述

实现了一个完整的拼图游戏系统，包含以下功能：
- ✅ 将图片分割成 4 块（2x2 网格）
- ✅ 随机打乱拼图块位置
- ✅ 拖拽移动拼图块
- ✅ 自动检测拼图完成
- ✅ 关卡系统（支持多张图片）
- ✅ 完成动画效果

## 文件说明

### 1. `PuzzlePiece.ts` - 拼图块组件
- 负责单个拼图块的显示和交互
- 处理拖拽逻辑
- 显示图片的一部分

### 2. `PuzzleManager.ts` - 拼图管理器
- 管理整个拼图游戏逻辑
- 创建和打乱拼图块
- 检测完成状态
- 处理关卡切换

### 3. `GameUI.ts` - 游戏UI控制器
- 初始化拼图游戏
- 处理拼图完成回调

## 使用步骤

### 步骤 1：创建拼图块预制体

1. **创建预制体节点**
   - 在场景中创建一个 Node，命名为 `PuzzlePiece`
   - 添加 `Sprite` 组件
   - 添加 `UITransform` 组件（设置合适的大小）
   - 添加 `PuzzlePiece` 组件脚本

2. **配置预制体**
   - 将 `PuzzlePiece` 节点拖拽到资源管理器，创建为预制体
   - 在预制体中，将 `Sprite` 组件赋值给 `PuzzlePiece` 脚本的 `sprite` 属性

### 步骤 2：设置拼图容器

1. **创建容器节点**
   - 在场景中创建一个 Node，命名为 `PuzzleContainer`
   - 添加 `UITransform` 组件
   - 设置合适的大小（例如：400x400）

2. **添加拼图管理器**
   - 在 `PuzzleContainer` 节点上添加 `PuzzleManager` 组件
   - 将 `PuzzleContainer` 节点赋值给 `puzzleContainer` 属性
   - 将 `PuzzlePiece` 预制体赋值给 `piecePrefab` 属性

### 步骤 3：配置 GameUI

1. **在 GameUI 中设置**
   - 将 `PuzzleManager` 节点赋值给 `puzzleManager` 属性
   - 设置 `puzzleImage`（SpriteFrame）或确保 `picNode` 有 Sprite 组件

### 步骤 4：准备图片资源

1. **方式一：直接指定图片**
   - 在 `GameUI` 的 `puzzleImage` 属性中直接指定 SpriteFrame

2. **方式二：从节点获取**
   - 确保 `picNode` 有 `Sprite` 组件并设置了 SpriteFrame

3. **方式三：动态加载（多关卡）**
   - 在 `PuzzleManager` 的 `imagePaths` 数组中添加图片路径
   - 格式：`'images/puzzle/puzzle1/spriteFrame'`

## 代码示例

### 基本使用

```typescript
// 在 GameUI.ts 中
// 已经自动初始化，无需额外代码
```

### 自定义关卡图片

```typescript
// 在 PuzzleManager.ts 中修改 imagePaths
private imagePaths: string[] = [
    'images/puzzle/level1/spriteFrame',
    'images/puzzle/level2/spriteFrame',
    'images/puzzle/level3/spriteFrame',
];
```

### 自定义完成回调

```typescript
// 在 GameUI.ts 中
private onPuzzleComplete(level: number) {
    console.log(`完成第 ${level} 关！`);
    
    // 播放音效
    // 显示奖励
    // 显示广告等
}
```

## 拼图布局

拼图块索引布局（2x2 网格）：
```
0  1
2  3
```

- 索引 0：左上角
- 索引 1：右上角
- 索引 2：左下角
- 索引 3：右下角

## 功能特性

### 1. 图片分割
- 自动将原图分割成 4 块
- 每块显示原图的 1/4 部分
- 使用 SpriteFrame 的 rect 属性实现裁剪

### 2. 随机打乱
- 游戏开始时自动打乱拼图块位置
- 确保有解（不会生成已完成的排列）

### 3. 拖拽交互
- 点击并拖拽拼图块移动
- 松开时自动吸附到最近的位置
- 如果位置被占用，会交换位置

### 4. 完成检测
- 实时检测所有拼图块是否在正确位置
- 完成后播放动画
- 自动进入下一关

### 5. 关卡系统
- 支持多张图片作为不同关卡
- 完成一关后自动加载下一关
- 可以自定义关卡数量

## 自定义选项

### 调整拼图块大小

在 `PuzzleManager.ts` 中修改位置计算：
```typescript
const cellWidth = width / 2;  // 可以调整
const cellHeight = height / 2;
```

### 调整拖拽吸附距离

在 `PuzzleManager.ts` 的 `checkPiecePosition` 方法中：
```typescript
if (nearestIndex >= 0 && minDistance < 100) {  // 调整 100 这个值
```

### 调整移动动画时长

在 `PuzzlePiece.ts` 的 `moveToPosition` 方法中：
```typescript
tween(this.node)
    .to(0.3, { position: position }, { easing: 'sineOut' })  // 调整 0.3
```

## 注意事项

1. **图片尺寸**
   - 建议使用正方形图片，便于分割
   - 图片尺寸最好是偶数，避免裁剪误差

2. **性能优化**
   - 如果图片很大，考虑压缩
   - 可以预加载下一关的图片

3. **触摸区域**
   - 确保拼图块节点有足够大的触摸区域
   - 可以在拼图块上添加一个透明的 Button 组件扩大触摸区域

## 扩展功能建议

1. **增加难度**
   - 3x3 网格（9 块）
   - 4x4 网格（16 块）

2. **添加音效**
   - 移动音效
   - 完成音效

3. **添加提示**
   - 显示原图缩略图
   - 高亮正确位置

4. **添加计时**
   - 记录完成时间
   - 最佳成绩

5. **添加撤销功能**
   - 记录移动历史
   - 支持撤销上一步

## 常见问题

### Q1: 拼图块显示不正确？
A: 检查图片资源是否正确加载，确保 SpriteFrame 有效。

### Q2: 拖拽不灵敏？
A: 检查节点的触摸区域大小，确保 UITransform 组件设置了合适的大小。

### Q3: 拼图块位置不对？
A: 检查 `PuzzleContainer` 的 UITransform 大小和位置计算是否正确。

### Q4: 如何添加更多关卡？
A: 在 `PuzzleManager.ts` 的 `imagePaths` 数组中添加更多图片路径。

## 总结

这个拼图游戏系统提供了完整的功能，可以直接使用。如果需要自定义，可以修改相应的参数和方法。祝开发顺利！

