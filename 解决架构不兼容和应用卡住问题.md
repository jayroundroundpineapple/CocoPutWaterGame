# 解决架构不兼容和应用卡住问题

## 问题分析

### 问题 1：架构不兼容
```
Error creating LLDB target: the specified architecture 'arm64-*-*' is not compatible with 'x86_64-apple-ios13.0.0-simulator'
```

**原因**：
- 在 Intel Mac 上运行 arm64 构建的应用
- 或者在 Apple Silicon Mac 上运行 x86_64 模拟器
- Xcode 项目架构配置不正确

### 问题 2：应用卡在启动界面
- 应用启动后卡在 Cocos Creator 图标界面
- 可能是脚本引擎初始化问题
- 或者资源加载问题

## 解决方案

### 方案 1：修复架构配置（在 Xcode 中）

#### 步骤 1：检查 Mac 架构
1. 打开终端，运行：
   ```bash
   uname -m
   ```
   - 如果显示 `arm64` → Apple Silicon Mac
   - 如果显示 `x86_64` → Intel Mac

#### 步骤 2：配置 Xcode 项目架构

1. **打开 Xcode 项目**
   - 用 Xcode 打开从 Cocos Creator 导出的 `.xcodeproj` 文件

2. **选择项目 Target**
   - 在左侧项目导航器中，点击项目名称（最顶部的蓝色图标）
   - 选择你的 Target（例如：`waterdemo-mobile`）

3. **配置 Build Settings**
   - 切换到 "Build Settings" 标签
   - 在搜索框中输入 "Architectures"
   - 找到 "Architectures" 设置
   - 选择 "Standard Architectures (Apple Silicon, Intel)" 或根据你的 Mac 选择：
     - **Apple Silicon Mac**：选择 `arm64`
     - **Intel Mac**：选择 `x86_64` 或 `Standard Architectures`

4. **配置 Excluded Architectures（模拟器）**
   - 在搜索框中输入 "Excluded Architectures"
   - 找到 "Excluded Architectures" 设置
   - 展开设置，找到 "Any iOS Simulator SDK"
   - 根据你的 Mac 类型设置：
     - **Apple Silicon Mac**：排除 `x86_64`（如果有）
     - **Intel Mac**：排除 `arm64`（如果有）

5. **配置 Valid Architectures**
   - 在搜索框中输入 "Valid Architectures"
   - 找到 "Valid Architectures" 设置
   - 确保包含：
     - **Apple Silicon Mac**：`arm64`
     - **Intel Mac**：`x86_64`

#### 步骤 3：选择正确的模拟器

1. **在 Xcode 顶部选择模拟器**
   - 点击设备选择器（显示当前设备的地方）
   - 选择与你的 Mac 架构匹配的模拟器：
     - **Apple Silicon Mac**：选择 "iPhone 14 Pro (arm64)" 等 arm64 模拟器
     - **Intel Mac**：选择 "iPhone 14 Pro" 等 x86_64 模拟器

2. **或者使用真机测试**
   - 连接 iPhone/iPad 真机
   - 选择真机作为运行目标

### 方案 2：检查应用初始化问题

#### 添加更多日志来定位问题

在 `AppDelegate.mm` 中添加更多日志，检查应用初始化流程：

```objective-c
- (BOOL)application:(UIApplication *)application didFinishLaunchingWithOptions:(NSDictionary *)launchOptions {
    NSLog(@"[AppDelegate] ========== didFinishLaunchingWithOptions 开始 ==========");
    
    // ... 现有代码 ...
    
    NSLog(@"[AppDelegate] 调用 AppDelegateBridge didFinishLaunchingWithOptions...");
    [appDelegateBridge application:application didFinishLaunchingWithOptions:launchOptions];
    
    // 添加：检查脚本引擎是否初始化
    dispatch_after(dispatch_time(DISPATCH_TIME_NOW, (int64_t)(0.5 * NSEC_PER_SEC)), dispatch_get_main_queue(), ^{
        se::ScriptEngine* se = se::ScriptEngine::getInstance();
        if (se) {
            NSLog(@"[AppDelegate] ScriptEngine 已创建，isValid: %d", se->isValid());
        } else {
            NSLog(@"[AppDelegate] WARNING: ScriptEngine 未创建");
        }
    });
    
    // ... 现有代码 ...
}
```

#### 检查资源加载

1. **检查资源路径**
   - 确认资源文件是否正确打包
   - 检查 `Info.plist` 中的资源路径配置

2. **检查脚本文件**
   - 确认 TypeScript 脚本是否正确编译
   - 检查是否有脚本加载错误

### 方案 3：清理并重新构建

1. **清理 DerivedData**
   ```bash
   rm -rf ~/Library/Developer/Xcode/DerivedData/waterdemo-*
   ```

2. **在 Xcode 中清理项目**
   - 菜单：Product → Clean Build Folder（`Shift + Cmd + K`）

3. **重新构建**
   - 菜单：Product → Build（`Cmd + B`）

4. **重新运行**
   - 选择正确的模拟器或真机
   - 运行项目

### 方案 4：检查 Cocos Creator 项目设置

1. **打开 Cocos Creator**
   - 打开项目

2. **检查构建设置**
   - 菜单：项目 → 项目设置 → 原生开发环境
   - 检查 iOS 相关设置

3. **重新导出 Xcode 项目**
   - 菜单：项目 → 构建发布
   - 选择 iOS 平台
   - 点击 "构建"
   - 确保选择正确的架构

## 快速检查清单

- [ ] Mac 架构类型（Apple Silicon 还是 Intel）
- [ ] Xcode 项目架构配置正确
- [ ] 选择了正确的模拟器（匹配 Mac 架构）
- [ ] Excluded Architectures 配置正确
- [ ] 清理了 DerivedData
- [ ] 重新构建了项目
- [ ] 检查了应用日志，确认初始化流程

## 常见问题

### Q1: 如何知道我的 Mac 是 Apple Silicon 还是 Intel？
A: 在终端运行 `uname -m`，或点击 Apple 菜单 → 关于本机，查看芯片类型。

### Q2: 为什么应用卡在启动界面？
A: 可能的原因：
1. 脚本引擎未正确初始化
2. 资源文件未正确加载
3. 架构不匹配导致崩溃（但被捕获）
4. 初始化代码中有阻塞操作

### Q3: WebAssembly 警告会影响应用吗？
A: 通常不会，这只是警告。但如果应用卡住，可能是其他问题。

## 推荐操作顺序

1. **首先修复架构问题**（方案 1）
2. **清理并重新构建**（方案 3）
3. **如果仍然卡住，添加日志检查**（方案 2）
4. **最后检查 Cocos Creator 设置**（方案 4）

